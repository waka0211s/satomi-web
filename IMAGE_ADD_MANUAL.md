# 画像追加マニュアル — En Rilievo NEJICO / satomi-web

**確定日：2026年9月1日**
**対象：Selected Works（作品）／ Piccolo Fiore（小物ライン）への写真追加**

このファイルは、サイトへの画像追加を「毎回ゼロから説明しなくても良い」ようにするための手順書です。
新しいチャットでクロちゃんに作業を頼むときは、このファイルを先に読み込んでもらってください。

---

## 0. 前提（1回だけ準備済み）

satomi-web フォルダの中には、以下の2つのスクリプトが既に置いてあります。

| ファイル | 役割 |
|---|---|
| `add_works.py` | **Selected Works**（Edition・番号付き・大作）に画像を追加 |
| `add_piccolo.py` | **Piccolo Fiore**（小物ライン・番号なし）に画像を追加 |

どちらも「専用フォルダに写真を入れて実行するだけ」で、
画像の圧縮・HTMLへの挿入・GitHubへの公開まで全自動で行います。

新しいラインを増やす場合は、この2つと同じ構造のスクリプトをクロちゃんに作ってもらってください
（`setup_◯◯_section.py` でセクションの土台を作る → `add_◯◯.py` で画像追加、の2段階セット）。

---

## 1. 使い方（毎回これだけ）

### ステップ1：ターミナルを開いて、必ず satomi-web フォルダに移動する

```bash
cd ~/Desktop/satomi-web
```

**⚠️ 最重要：この `cd` を忘れると、スクリプトが違う場所（ホームフォルダ）に作られたり、
「ファイルが見つかりません」というエラーが出ます。** 必ず最初にこれを打つこと。

プロンプトの表示が

```
sumito.wakayama@...MacBook-Air satomi-web %
```

のように **`satomi-web`** になっていることを確認してから、次に進む。

### ステップ2：追加したいラインのスクリプトを実行（1回目）

Selected Works に追加するなら：
```bash
python3 add_works.py
```

Piccolo Fiore に追加するなら：
```bash
python3 add_piccolo.py
```

写真がまだ何もない状態で実行すると、

```
📁 .../Desktop/new-works（または piccolo-fiore）を作りました。
```

と表示され、デスクトップに専用フォルダが自動生成される。

### ステップ3：写真をフォルダに入れて、リネームする

デスクトップにできたフォルダ（`new-works` または `piccolo-fiore`）に、
追加したい写真をドラッグ&ドロップ。

**ファイル名は必ずこの形にする：**

```
英名_和名.jpg
```

例：`Camellia_椿.jpg` / `OrangeRose_薔薇.jpg`

- アンダーバー `_` の前が英語名、後が日本語名
- 拡張子は `.jpg` または `.jpeg`（二重に付けないよう注意。`Sakura_桜.jpg.jpeg` のような誤りに注意）
- 同じ花名が被る場合は末尾に数字でOK（`Rose2_薔薇.jpg`）

### ステップ4：もう一度スクリプトを実行（本番）

ステップ2と同じコマンドを、もう一度実行する。

```bash
python3 add_works.py
```
または
```bash
python3 add_piccolo.py
```

今度は写真が入っているので、以下が自動で走る：

1. 長辺1200〜1600px・画質80%に圧縮（`sips` を使用、追加インストール不要）
2. `images/` フォルダにコピー
3. `satomi_index.html` / `satomi_en.html` の該当セクションに挿入
4. `git add` → `git commit` → `git push`（GitHub Pagesへ反映）

最後に

```
🌸 完了しました。1〜2分後にサイトへ反映されます。
```

と出れば成功。1〜2分待ってから、下記URLで確認する。

```
https://waka0211s.github.io/satomi-web/satomi_index.html
```

---

## 2. よくあるトラブルと直し方

### 「No such file or directory」と出る

→ ステップ1の `cd ~/Desktop/satomi-web` を忘れている、または違う場所で実行している。
`ls` で今いる場所に `satomi_index.html` があるか確認する。

### ファイルが違う場所に作られてしまった

→ ホームフォルダ（`~`）に間違って作られた場合、以下で正しい場所に移動できる。

```bash
mv ~/ファイル名.py ~/Desktop/satomi-web/
```

### ターミナルの入力欄に前の文字が残っていて変なエラーが出る

→ `command + A` で全選択 → `delete` で消してから、次のコマンドを打つ。

### 画像が表示されない（404になる）

→ HTML側の `<img src="...">` が `images/` フォルダを参照できていない可能性。
`satomi_index.html` 内の該当画像パスに `images/` が付いているか確認する。

---

## 3. 【最重要・二度と忘れないこと】GitHubの公開範囲について

`add_works.py` / `add_piccolo.py` は、実行の最後に **`git add -A`** を行う。

**`-A` は「satomi-web フォルダの中にある、全てのファイル・フォルダ」を対象にする。**

つまり、`satomi-web` フォルダの中に、名刺データ・内部資料・非公開の仕様書などを
うっかり置いてしまうと、**画像を1枚追加しただけのつもりで、それらも一緒に
公開GitHubリポジトリへ送信されてしまう。**（2026年9月1日、実際にこの事故が発生し、
`git filter-repo` を使って履歴から完全に削除する対応を行った。）

### 鉄則

> **`satomi-web` フォルダの中には、"公開してよいもの" だけを置く。**
> 名刺・仕様書・非売品カードのデザインファイルなどは、
> デスクトップの別の場所（`satomi-web` の外）で管理する。

`.gitignore` によって、以下は今後 `git add -A` をしても自動的に除外される設定済み：

```
satori_business_card_2/
satori_business_card_2 2/
satori_business_card_2.zip
名刺関連/
文書ファイル/
商品カード（製造番号）/
NUMBERING_AND_CARD_SPEC.md
*.bak
.DS_Store
```

ただし、**これは「今登録されているフォルダ名」しか守ってくれない。**
新しく非公開フォルダを作った場合は、都度 `.gitignore` に追記する必要がある。
迷ったら、クロちゃんに「このフォルダ、gitignoreに入れておいて」と一言で頼めばよい。

---

## 4. 命名・デザインの原則（振り返り用）

- 番号は希少性の通貨。小物ライン（Piccolo Fiore等）には番号を付与しない
- Piccolo Fiore は「Selected Worksの延長」ではなく「一段軽いトーン」の別セクション
- 呼称ひとつで価格帯の認識が変わる（「ポストカード」ではなく独自の呼び名を使う）
- 詳しい番号体系・カード仕様は `NUMBERING_AND_CARD_SPEC.md` を参照

---

*このファイルはプロジェクトの参照ドキュメントとして保存してください。
内容が古くなった場合は、クロちゃんに「マニュアルを更新して」と頼めば差分を反映できます。*
